// override store defaults:
let fwStoreState = {
    uioptions: { edit: { is_validation_summary: true } }
};
// override store getters:
let fwStoreGetters = {
};
// override store actions:
let fwStoreActions = {
};
<~/common/vue/store.js>
