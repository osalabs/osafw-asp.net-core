## What changed
- updated quick search event handler and added Vue header quick search inputs
- routed Vue quick search selections to GoAction for view/edit navigation
- relocated autocomplete script includes to controller-specific load_script templates

## Commands that worked (build/test/run)
- not run (not requested)

## Pitfalls - fixes
- none

## Decisions - why
- use GoAction redirects in Vue headers to reuse server-side navigation behavior
- keep autocomplete library loading scoped to controller templates that use quick search

## Heuristics (keep terse)
- none
