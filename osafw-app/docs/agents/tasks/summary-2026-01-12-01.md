## What changed
- revised view-only field styling to rely on standard control sizing tokens and simplified view value tokens
- removed fw-bold/fw-semibold classes from view screen labels and kept semibold weight in #FView styling

## Commands that worked (build/test/run)
- 

## Pitfalls - fixes
- 

## Decisions - why
- used existing control padding/radius tokens to avoid introducing extra view-specific variables
- kept label weight in CSS to apply consistently across view screens without template classes

## Heuristics (keep terse)
- 
